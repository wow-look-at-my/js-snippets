// The Go half of ui/timeline-wire — the ENCODER. A nested module so the
// library's root stays pure TypeScript and a Go consumer pulls only this.
module github.com/wow-look-at-my/js-snippets/timelinewire

go 1.25.0
