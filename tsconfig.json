{
  "//": "Editor/IDE config only. ts0 generates its own tsconfig for the build's type-check; this mirrors it (bundler resolution, no emit) so editors agree with CI.",
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "allowImportingTsExtensions": true
  },
  "include": ["src/**/*.ts", "wgsl.d.ts"]
}
